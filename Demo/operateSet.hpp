//
//  operateSet.hpp
//  Demo
//
//  Created by xdf on 2024/7/22.
//

/*
 C++ 标准库中的 <set> 是一个关联容器，它存储了一组唯一的元素，并按照一定的顺序进行排序。
 
 <set> 提供了高效的元素查找、插入和删除操作。它是基于红黑树实现的，因此具有对数时间复杂度的查找、插入和删除性能。

 <set> 容器中存储的元素类型必须满足以下条件：

 元素类型必须可以比较大小。
 元素类型必须可以被复制和赋值。
 */
#ifndef operateSet_hpp
#define operateSet_hpp

void operateSet();

#endif /* operateSet_hpp */
